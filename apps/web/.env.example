# Local combined surface. Production Vercel MUST use public; admin container uses internal.
DEPLOYMENT_SURFACE=local
API_INTERNAL_URL=http://127.0.0.1:8180
# Never put API keys, database credentials, or Gophish admin credentials into NEXT_PUBLIC_*.
